export const VERSION = process.env.NEXT_PUBLIC_VERSION; // based on the version in package.json
export const ENV = process.env.NEXT_PUBLIC_ENV; // development, production
export const NAME = process.env.NEXT_PUBLIC_NAME; // package.json -> name
